name: Build and Package

on:
  push:
    branches:
      - main

jobs:
  build_and_pack:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ vars.DOTNET_VERSION }}

      - name: Get Project Version
        id: get_version
        uses: mod-posh/GetProjectVersion@main
        with:
          Filename: 'Directory.Build.props'
          verbose: "verbose"
      - name: Compose preview version parts
        shell: pwsh
        run: |
          $sha7 = "${env:GITHUB_SHA}".Substring(0,7)
          echo "VERSION_PREFIX=${{ steps.get_version.outputs.version }}" >> $env:GITHUB_ENV
          echo "VERSION_SUFFIX=preview.${env:GITHUB_RUN_NUMBER}-g$sha7" >> $env:GITHUB_ENV

      - name: Clean
        run: dotnet clean ${{ github.workspace }}/${{ vars.PROJECT_NAME }}/${{ vars.PROJECT_NAME }}.sln --configuration Release
      - name: Restore dependencies
        run: dotnet restore ${{ github.workspace }}/${{ vars.PROJECT_NAME }}/${{ vars.PROJECT_NAME }}.sln
      - name: Build
        run: >
          dotnet build ${{ github.workspace }}/${{ vars.PROJECT_NAME }}/${{ vars.PROJECT_NAME }}.sln
          --configuration Release
          --no-restore
          /m:1
          -p:ContinuousIntegrationBuild=true
          -p:UseSharedCompilation=false
      - name: Package
        run: >
          # Pack Core Library first (since others depend on it)
          dotnet pack ${{ github.workspace }}/Xml2Doc/src/Xml2Doc.Core/Xml2Doc.Core.csproj
          --configuration Release
          --output ./publish
          --no-build
          -p:ContinuousIntegrationBuild=true
          -p:VersionPrefix=${{ env.VERSION_PREFIX }}
          -p:VersionSuffix=${{ env.VERSION_SUFFIX }}

          # Pack CLI Tool
          dotnet pack ${{ github.workspace }}/Xml2Doc/src/Xml2Doc.Cli/Xml2Doc.Cli.csproj
          --configuration Release
          --output ./publish
          --no-build
          -p:ContinuousIntegrationBuild=true
          -p:VersionPrefix=${{ env.VERSION_PREFIX }}
          -p:VersionSuffix=${{ env.VERSION_SUFFIX }}

          # Pack MSBuild Package
          dotnet pack ${{ github.workspace }}/Xml2Doc/src/Xml2Doc.MSBuild/Xml2Doc.MSBuild.csproj
          --configuration Release
          --output ./publish
          --no-build
          -p:ContinuousIntegrationBuild=true
          -p:VersionPrefix=${{ env.VERSION_PREFIX }}
          -p:VersionSuffix=${{ env.VERSION_SUFFIX }}

      - name: Push to NuGet
        run: dotnet nuget push ./publish/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
