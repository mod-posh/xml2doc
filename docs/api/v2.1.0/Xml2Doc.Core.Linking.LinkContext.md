# LinkContext

Ambient context for resolving a cref into a Markdown link.

<a id="xml2doc.core.linking.linkcontext.#ctor(string,bool,string)"></a>

## Method: #ctor(string, bool, string)

Ambient context for resolving a cref into a Markdown link.
