# FileTemplateRenderer

Applies an optional token-based template and optional front-matter file.

<a id="xml2doc.core.templates.filetemplaterenderer.#ctor(string,string)"></a>

## Method: #ctor(string, string)

Loads the configured files once for deterministic rendering.

<a id="xml2doc.core.templates.filetemplaterenderer.render(xml2doc.core.templates.templaterendercontext)"></a>

## Method: Render(TemplateRenderContext)

Transforms one complete built-in Markdown document.
