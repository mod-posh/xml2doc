# IAliasProvider

Applies display aliases to type tokens used in signatures, labels, and anchors.

<a id="xml2doc.core.aliasing.ialiasprovider.applyaliases(string)"></a>

## Method: ApplyAliases(string)

Applies aliases to complete type tokens without modifying partial identifiers.

**Parameters**

- `value` — The signature, label, or documentation identifier to transform.

**Returns**

The transformed value.
