# DocumentPlanEntry

Associates one logical document with its validated output path.

<a id="xml2doc.core.paths.documentplanentry.#ctor(xml2doc.core.documentdescriptor,string)"></a>

## Method: #ctor(DocumentDescriptor, string)

Associates one logical document with its validated output path.

**Parameters**

- `Document` — Immutable document identity and metadata.
- `Path` — Canonical output-root-relative logical path using `/`.

<a id="xml2doc.core.paths.documentplanentry.document"></a>

## Property: Document

Immutable document identity and metadata.

<a id="xml2doc.core.paths.documentplanentry.path"></a>

## Property: Path

Canonical output-root-relative logical path using `/`.
