# DocumentPathContext

Provides deterministic inputs to a document path resolver.

<a id="xml2doc.core.paths.documentpathcontext.#ctor(xml2doc.core.documentdescriptor,string,string)"></a>

## Method: #ctor(DocumentDescriptor, string, string)

Provides deterministic inputs to a document path resolver.

**Parameters**

- `Document` — The logical document being placed.
- `DefaultPath` — The backward-compatible flat logical path.
- `FileName` — The deterministic filename for the document.

<a id="xml2doc.core.paths.documentpathcontext.defaultpath"></a>

## Property: DefaultPath

The backward-compatible flat logical path.

<a id="xml2doc.core.paths.documentpathcontext.document"></a>

## Property: Document

The logical document being placed.

<a id="xml2doc.core.paths.documentpathcontext.filename"></a>

## Property: FileName

The deterministic filename for the document.
