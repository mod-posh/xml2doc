# ADR‑001 — Scope and Non‑Goals

## Decision

xml2doc is responsible for transforming .NET XML documentation into Markdown.

The project will not expand into a full documentation platform.

## Consequences

The design remains focused on deterministic Markdown generation.
